import 'package:flutter/material.dart';

class AssetVideoBackground extends StatelessWidget {
  const AssetVideoBackground({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
